DAG là đồ thị có hướng không có chu trình .

đề bài cho không có cổ đông nào có số cổ phiếu âm. nhưng có thể khi bạn mod mà về còn 1 xong lại -100 thì sẽ tồn tại số âm. nên hãy trừ rồi %mod +mod %mod. để ra kq đúng

nếu đề cho chọn k mảng con mà k<1e5 thì dùng mô phỏng 

trong map, hoac unordered_map, chi can goi m[x] thi tu khac se se them x vao map.

với bài toàn string hãy thử nghĩ đến việc gấp đôi chuỗi.
ví dụ với bài hỏi chuỗi s có được tạo bằng cách lặp nhiều lần của một chuỗi con nào đó trong S không?
thì chỉ cần gấp gấp đôi chuỗi, rồi từ vị trí (1,s.size()-2) nếu xuất hiện s thì trả về true

nếu string.find(s) = string::npos thì không tồn tại chuỗi trong dãy. hàm find trả về string::npos